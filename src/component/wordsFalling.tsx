import React, { useEffect, useRef} from 'react';
import { Engine } from 'matter-js'

function wordsFailling() {
  const scene = useRef()
  const engine = useRef(Engine.create())
  return (
    <>

    </>
  )
}

export default wordsFailling;